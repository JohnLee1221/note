## gdb 指令

```shell
(gdb) break main  # 设置断点在 main 函数
(gdb) run         # 运行程序，程序会停在 main 函数的断点处
(gdb) step        # 进入 main 函数内部
(gdb) step        # 执行下一行代码，进入某个函数的定义
(gdb) finish      # 退出当前函数，返回到调用处
```

